# it is OK to use multiple qualifiers and document ids

INSERT Customer; uid[unique=true];defaultPaymentAddress( &addrID ); defaultShipmentAddress( &addrID )
; andi; addr1 ; addr2
 
INSERT Address; &addrID ; Town; owner( Customer.uid );
; addr1 ; Wroclaw ; andi ;
; addr2 ; Wronki; andi ;